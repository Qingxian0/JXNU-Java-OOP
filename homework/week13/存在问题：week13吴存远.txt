开关灯：FrameShow.modifyComponentSize(this, 3,3);
  很不错，做了自己的探索。这种自适应，应该是自定义组件与窗体的大小比例。
  但要注意：更改组件字体，也能起到类似效果。

排序数据输出：首先，你是唯一一个把界面做的较好的，值得表扬。
但要注意：beforeString.split("\\s+"); 用\s（即空白）分隔很好，但要考虑到用户数据间可能用逗号（建议尝试下s.split("[^\\d\\.\\-]+");）。还有，用户可能故意输入错误数据，要考虑加上异常处理：数据格式不正确时，在结果文本框显示错误信息。

滚动字幕：完全动不起来。感觉这块你想的复杂了。

其他没有问题，总体很不错。
